# 概要 — 現状仕様 & ロードマップ

ランナー向けの 2 機能を持つ Next.js アプリ。計算は自前（OpenStreetMap + アルゴリズム）、
描画は MapLibre、保存はブラウザ(localStorage)。このファイルが「**現状の全体像**」と
「**今後やりたいこと**」の単一の入口。詳細は各 spec を参照。

更新日: 2026-06-06

## ドキュメント
- 周回路の仕様: [spec-round-trip.md](./spec-round-trip.md)
- トレーニング計画の仕様: [spec-training-planner.md](./spec-training-planner.md)
- トレーニング計算ロジック（全決定ルール）: [training-plan-logic.md](./training-plan-logic.md)
- Garmin/Strava 連携の調査: [garmin-strava-integration.md](./garmin-strava-integration.md)
- MTG メモ（理想・着想）: [mtg.md](./mtg.md)

---

## 現状の機能（実装済み）

### A. 周回路 / 距離計算（`/`）
- 論文 Lewis & Corcoran 2024 の「等時線多角形法 + 多目的パレート局所探索」を TS 実装
- OSM/Overpass 取得 → 有向グラフ → Dijkstra。**detour 補正**（実距離/周長を実測して周長を調整）＋
  各候補の再スケールで、全候補を**目標距離 ±約10%以内**に（例: 10km→全候補 9.8〜10.2km）
- 多目的: f1=距離誤差 / f2=区間重複%。パレートフロント + 方位別の別方向候補
- 描画: MapLibre(キー不要 OSM タイル)。選択経路=太線/他候補=薄線、始点マーカー
- 始点指定: 地図クリック / 緯度経度 / プリセット / **現在地(GPS→失敗時 IP 概算)** / **自宅登録(localStorage)**
- エクスポート: **GPX をスマホに送る/保存(Web Share)**、Google マップ近似リンク(経由地点を RDP で曲がり角優先)
- プロファイル walk 固定（ランニング想定。API は bike も受付）

### B. トレーニング計画（`/training`）
- 取込: **Garmin CSV** / **Strava 連携(OAuth, 任意)**
- 走力推定: 週間距離・最長走・イージーペース・**現VDOT(≈VO2max)**・**最大HR(自動検出)**
  （VDOT は最大HRがある時「追い込んだ走」優先で精度UP）
- 生成: 期分け(基礎→構築→ピーク→調整)でロング走を漸進。**最長走×2+2km の安全上限**
- 週設定: 練習可能な曜日 + **週の練習回数(分散選択)** + 確保時間(距離を時間でキャップ)
- 各日: 距離・推定時間・**ペース(@x:yy/km)**・**HRゾーン(Z1–5)**・**週1のポイント練習★**
- 目標タイム → **VDOT(Daniels) からペース設計**、「**目標と伸ばし方**」(現状VDOT→目標, 実現可能性)
- スキップ / 今週スキップ / 再計算(今日起点)
- 共通: ヘッダーナビ、設定は localStorage 保存

---

## 既知の制約
- Google マップ(一般アプリ)は任意ラインのナビ不可 → 正確なナビは GPX + 専用アプリ(OsmAnd/Komoot/Garmin)
- VO2max は数値ソースが無く**推定**(VDOT)。最大HRは実測テスト無しの**観測値**で近似
- 周回路の計算は密な市街地・長距離で重い(東京 10km で約15秒)
- Overpass はフェアユース(公開エンドポイント)。多用時は自前/キャッシュ推奨
- 保存はブラウザ単位(端末/ブラウザ間で共有されない)
- Strava 連携は**キー設定で有効化**(未設定なら未連携表示)。実トークン交換は要キー検証

---

## 今後やりたいこと（ロードマップ）

### 1. 心拍・VO2max の深掘り（MTG の理想に対応）
- [ ] **効率トレンド**: 同一心拍でのペース推移(aerobic efficiency / HR-pace 乖離)を可視化＝「伸びの実測」
- [ ] **継続モニタリング → 提案**: 取込のたびに走力を再評価し、改善提案・メニュー再構成を提示
- [ ] **目標の逆提案**: 実現可能性が「厳しい」時、現実的な目標タイムを逆算提案
- [ ] **インターバルの具体化**: 「1000m×5」等、本数×距離×レストのメニュー化
- [ ] 気温/標高補正でペース・心拍を解釈

### 2. 目標設定の支援（MTG）
- [ ] 直近レース/ベスト走から妥当な目標レンジを提示し、目標設定を補助

### 3. データ連携
- [ ] **Strava 実連携の有効化・検証**(キー投入後の取込まで通す)
- [ ] (参考) Garmin 公式 API は法人審査で個人非現実的 → Strava 経由が現実解

### 4. 周回路の改善
- [ ] 計算時間の短縮(取得半径/refine の調整、early-exit)
- [ ] Overpass レスポンスのキャッシュ
- [ ] 標高/緑地を目的関数に追加(論文の将来課題)
- [ ] **2機能連携**: トレーニングのロング走を周回路として実コース生成

### 5. 地図・出力
- [ ] Google Maps JS SDK でGoogleタイル上に**正確な線**を描画(要・課金キー)
- [ ] iCal エクスポート、達成率の可視化

### 6. 運用
- [ ] サーバー保存/アカウント(端末間同期)
- [ ] 朝ランの示唆など運用 Tips(MTG)

---

> 注: 個別 spec は詳細の正典だが、最近の追加(detour補正/GPX・共有/RDP/VO2max・HR/Strava足場)は
> このファイルを最新として扱う。spec への反映は順次。
